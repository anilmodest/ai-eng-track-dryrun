# Routes

Week 0 has no route: it is where yours is assigned. From Week 1, every week's folder has
`routes/start.md`, `routes/core.md` and `routes/pro.md` (what that route is given and asked), and
`routes/worked_example.py` for the start route. `make route ROUTE=...` applies yours.
