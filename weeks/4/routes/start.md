# Week 4 on the start route

**Faults named, worked example given.** `run_agent` is a stub whose comments name every wall and
checkpoint. `routes/worked_example.py` runs *one* tool call and finishes: the loop's body without
the loop. Build the loop around it.
