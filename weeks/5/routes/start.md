# Week 5 on the start route

**Faults named, worked example given.** `app/guard.py` is a pass-through whose docstrings name
each defence and what it stops. `routes/worked_example.py` fences a `<passage>` and strips one
pattern with a test. Tracing (`app/trace.py`) is given and working on this route.
