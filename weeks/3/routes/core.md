# Week 3 on the core route

**Faults present, not located, count not given.** `/ask` is implemented with mistakes planted:
at least one turns an outage into an abstention, which no metric will show you. Read it as the
person on call would.
