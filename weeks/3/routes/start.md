# Week 3 on the start route

**Faults named, worked example given.** The `/ask` stub names each of the two gates and the
citation mapping in its comments. `routes/worked_example.py` answers a question from a single
passage with the `grounded` flag and one citation: the whole shape at k=1. Build from it.
