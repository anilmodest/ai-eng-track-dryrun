# Week 2 on the core route

**Faults present, not located, count not given.** `metrics.py` and `by_heading` are implemented
with mistakes planted. One of them makes a number that looks plausible and is wrong; the gate
does not catch every one. Review before you run.
