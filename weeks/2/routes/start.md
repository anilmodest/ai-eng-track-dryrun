# Week 2 on the start route

**Faults named, worked example given.** The metric functions raise with a message naming what
each must return; `routes/worked_example.py` implements a sibling metric (mean average precision)
and a sibling chunker (`by_lines`) in full. Copy the shape, not the code. Run it first.
