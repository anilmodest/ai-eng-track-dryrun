# Week 1 on the start route

**Faults named, worked example given.** `app/api/extract.py` is a stub whose comments name each
thing that goes wrong if you skip it. `routes/worked_example.py` is the same five requirements
solved for a *different, smaller* endpoint (language detection with a two-field schema): read it,
run it, then build yours the same way. `retry.py`, `structured.py` and `cost.py` are given and
working. Use an unblock call as soon as you are stuck for more than an hour.

```
uv run python weeks/1/routes/worked_example.py      # runs against the fake provider, no key
```
